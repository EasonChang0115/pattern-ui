import DesignInputNumber from './components/designInputNumber.vue';
DesignInputNumber.install = (app) => {
  app.component(DesignInputNumber.name, DesignInputNumber);
};
export default DesignInputNumber;
